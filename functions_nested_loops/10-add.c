#include "main.h"
/**
 * print_last_digit - add print_last_digit return printer.
 *
 *@number: use a to print the absolute value
 *
 * Return: lastdigit
 */
int add(int a, int b)
{
		int add = a + b;

		if ( a == 89)
		     {
			if ( b == 9)

			{
                         return(add);
		     }
	   }
}
