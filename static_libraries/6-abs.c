#include "main.h"
/**
 * _abs - add _abs return printer.
 *
 *@a: use a to print the absolute value
 *
 * Return: 1 or 0
 */
int _abs(int a)
{

	if (a < 0)

	{
		a = (a * -1);
		return (a);
	}
	else
	{
		return (a);
	}
}
