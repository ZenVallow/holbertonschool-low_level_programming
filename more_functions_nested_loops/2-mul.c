#include "main.h"

/**
 * mul - check the code
 *
 *@a: add multiple value with a and b
 *
 *@b: same
 *
 *@sum: same
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int sum = a * b;

	if (a == 98 && b == 1024)

	{

		return (sum);

	}

	if (a == -402 && b == 4096)

	{

		return (sum);

	}

	return (0);
}
